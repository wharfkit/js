Stable releases are blocked while this file exists. scripts/release.ts refuses to bump or publish any version without an rc suffix, so every release lands on the npm dist-tag "next" and no package's "latest" pointer moves.

Remove this file in a reviewed PR when the go/no-go checkpoint approves the first stable release (4.0.0 to "latest").
